export const DB_NAME = "ecommerse-admin";

// these option are allowing that cookie is modifiable only on server not on user mechine
export const cookiesOptions = {
  httpOnly: true,
  secure: true,
};
